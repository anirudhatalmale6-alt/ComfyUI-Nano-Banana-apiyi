# requirements.txt

# For loading the GOOGLE_API_KEY from your .env file
python-dotenv

# The official Google AI Python SDK
google-genai

# VertexAI dependencies (only needed if using PROJECT_ID + LOCATION approach)
# Uncomment these if you need VertexAI support:
# google-cloud-aiplatform
# google

# For image processing (PIL) - already installed with ComfyUI
# Pillow

# Core Dependencies - already installed with ComfyUI
# torch
# numpy
